# L'Orchidee ETL - daily run at 02:00 server time.
# Install with: crontab -e   (as the user that owns the venv/logs)
#
# Adjust the path to wherever you deployed the lorchidee-etl folder on the VPS.
0 2 * * * /opt/lorchidee-etl/cron/run_etl.sh
